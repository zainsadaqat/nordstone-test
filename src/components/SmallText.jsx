import React from 'react';

const SmallText = ({ text }) => {
  return (
    <p className="tracking-wide font-medium text-[10px] text-[#ADADAD]">
      {text}
    </p>
  );
};

export default SmallText;
